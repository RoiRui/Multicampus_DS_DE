## 📂 파일 입출력

#### 📌 파일 열기
- 파일 입출력 작업을 하기 위해서 open() 함수 사용
- 형식 : open(파일명, 파일 열기 모드)

| 파일 열기 모드 |             설명             |
| :------------: | :--------------------------: |
|       r        |      read : 읽기만 가능      |
|       w        |     write : 쓰기만 가능      |
|       a        | append : 파일 끝에 내용 추가 |

#### 📌 파일 생성하기
- 파일을 쓰기 모드(w)로 연다
- 해당 파일이 존재하지 않으면 새로 생성하고 존재한다면 덮어쓴다 (기존 내용 삭제)

```python
f = open("file1.txt", 'w')
f.close()
```

- 현재 폴더 외 다른 폴더에 저장하고 싶다면 전체 경로를 적으면 된다

```python
f = open("D:/python/file1.txt", 'w') ※ / or \\만 사용 가능 (\ 사용 안됨)
f.close()
```

#### 📌 파일 닫기
- 파일 작업이 끝나면 close() 함수로 열려 있는 파일 객체를 닫는다
- 프로그램 종료 시 열려 있는 파일 객체는 자동으로 닫히지만 쓰기 모드로 연 파일을 닫지 않고 다시 작업할 경우 에러가 발생하므로 close()로 닫는 것이 좋다

#### 📌 파일에 쓰기
- 파일을 쓰기 모드(w)로 열고 파일 객체의 write() 함수로 출력값을 파일에 기록한다
- 형식 : f.write(데이터)

#### 📌 파일 생성 시 인코딩을 UTF-8로 설정

```python
data = "안녕하세요"

f = open("file1.txt", 'w', encoding = 'utf-8') ※ 디폴트 값 : ANSI
f.write(data)
f.close()
```

#### 📌 CSV 파일로 저장하기
- CSV 형식 파일이란?
  - 콤마(,)로 필드가 구분되어지는 형식의 파일
- CSV 파일로 저장하기
  - 리스트 각 요소마다 콤마 붙여서 출력
  - 마지막 요소에만 안 붙임 (if 문 사용하여 마지막 구별)

#### 📌 파일 읽기
- readline()
  - 1개 행씩 읽어오기
  - 1행 끝에 '\n' 포함
- readlines()
  - 모든 행을 읽어 라인 별로 잘라서 리스트 생성
  - 1개 행이 1개 요소 : \['...', '...', '...', ..., '...'\]
- read()
  - 내용 전체를 읽어서 문자열로 반환
- 파일 읽기 인코딩 문제
  - ANSI로 저장된 한글 파일을 읽는 경우 : f =  open("test.txt", 'r')
  - UTF-8로 저장된 한글 파일을 읽는 경우 : f = open("test.txt", 'r', encoding = 'utf-8')

```python
※ 첫 번째 행 읽기

f = open("test.txt", 'r')
line = f.readline()
print(line) ➝ 안녕하세요, 홍길동입니다.
f.close()
```

```python
※ readline() 함수를 이용해서 전체 라인 읽기

f = open("test.txt", 'r')

while True:
  line = f.readline()
  if not line:
    break
  print(line) ➝ 안녕하세요, 홍길동입니다.
                 
                 지금 파이썬을 공부하고 있습니다.
                 
                 1. 함수
                 
                 2. 모듈
                 
                 3. 패키지

f.close()
```

```python
※ 1행 출력 후 줄바꿈 하지 않는다

f = open("test.txt", 'r')

while True:
  line = f.readline()
  if not line:
    break
  print(line, end = '') ➝ 안녕하세요, 홍길동입니다.
                           지금 파이썬을 공부하고 있습니다.
                           1. 함수
                           2. 모듈
                           3. 패키지

f.close()
```

```python
※ readlines() 함수를 이용해서 전체 라인 읽기

f = open("test.txt", 'r')
lines = f.readlines()
print(lines) ➝ ['안녕하세요, 홍길동입니다.\n', '지금 파이썬을 공부하고 있습니다.\n', '1. 함수\n', '2. 모듈\n', '3. 패키지\n']
f.close()
```

```python
※ 전체 라인 읽어서 1행씩 출력하기

f = open("test.txt", 'r')
lines = f.readlines()

for line in lines:
  print(line) ➝ 안녕하세요, 홍길동입니다.
                 
                 지금 파이썬을 공부하고 있습니다.
                 
                 1. 함수
                 
                 2. 모듈
                 
                 3. 패키지
  
f.close()
```

```python
※ 1행 출력 후 줄바꿈 하지 않는

f = open("test.txt", 'r')
lines = f.readlines()

for line in lines:
  print(line, end = '')

f.close()
```

```python
※ readlines() 없이 파일 읽어오기

f = open("test.txt", 'r')

for line in f:
  print(line, end = '')

f.close()
```

```python
※ read() 함수 예제

f = open("test.txt", 'r')
data = f.read()
print(data) ➝ 01234
               abcde
               가나다라마
print(type(data)) ➝ <class 'str'>
print(len(data)) ➝ 17

※ 1개 문자씩 출력

for ch in data:
  print(ch) ➝ 0
               1
               2
               3
               4
               
               a
               b
               c
               d
               e
               
               가
               나
               다
               라
               마
```

#### 📌 seek() 함수
- 파일 내에서 위치로 검색할 수 있는 함수
- 형식 : seek(offset, whence)
  - offset
    - 시작 위치로부터의 열의 위치
  - whence
    - 0 : 시작 위치
    - 1 : 현재 위치
    - 2 : 파일의 끝
- 예제 파일 내용

<img width=300 height=200 src="https://user-images.githubusercontent.com/106376150/178155827-9b9f960b-7c6c-4158-9c27-b0d5b478e0c9.png"/>

```python
f = open("test.txt", 'r')

f.seek(0, 0)
lines = f.readlines()
print(lines) ➝ ['01234\n', 'abcde\n', '가나다라마']
f.close()

f.seek(1, 0)
lines = f.readlines()
print(lines) ➝ ['1234\n', 'abcde\n', '가나다라마']
f.close()

f.seek(7, 0)
lines = f.readlines()
print(lines) ➝ ['abcde\n', '가나다라마']
f.close()

f.seek(14, 0)
lines = f.readlines()
print(lines) ➝ ['가나다라마']
f.close()

f.seek(16, 0)
lines = f.readlines()
print(lines) ➝ ['나다라마']
f.close()

f.seek(0, 0)
line = f.readline()
print(line, end = '') ➝ 01234
f.close()

f.seek(7, 0)
line = f.readline()
print(line, end = '') ➝ abcde
f.close()

f.seek(14, 0)
line = f.readline()
print(line, end = '') ➝ 가나다라마
f.close()
```

#### 📌 엔터 키(\r\n)
- \r : 커서를 행의 맨 왼쪽으로 이동
- \n : 커서를 한 칸 아래로 이동 (새로운 행 추가)

#### 📌 with 문으로 파일 열기
- with 문이 종료되면 파일 객체는 자동으로 close()
- 형식
```python
with open(파일명, 열기 모드) as 파일객체:
     수행문자
```

```python
with open("test.txt", 'w') as f:
     f.write("hello")
     
file = "test2.txt"
data = '''Python programming
R programming
Web programming'''

with open(file, 'w') as f:
     f.write(data)
```
