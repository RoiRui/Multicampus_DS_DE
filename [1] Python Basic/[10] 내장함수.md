## 📂 내장함수(Built-in-Fuction)

#### 📌 내장함수란?
- 파이썬에 이미 만들어져 내장되어 있는 함수들
- 별도의 모듈을 import하지 않고 사용가능
- 형식에 맞춰 함수이름만 호출해서 사용

#### 📌 파이썬의 내장함수

<p><img width=500 height=370 src="https://user-images.githubusercontent.com/106376150/178096944-9412ec77-215f-4997-a959-8fdfe8361857.jpg"></p>

- abs(x) : x의 절대값 반환

```python
print(abs(-10)) ➝ 10
```

- all(iterable) : 모든 요소가 참(0이 아닌 값)이면 True, 아니면(0) False

```python
print(all([1, 2, 3]) ➝ True
print(all([0, 1, 2, 3]) ➝ False
```

- any(iterable) : 하나라도 참이면 True, 모두 거짓이면 False

```python
print(any([0, 0, 0])) ➝ False
print(any([0, "", []]) ➝ False
print(any([0, 1, 0])) ➝ True
```

- chr(i) : ASCII 코드 값에 해당하는 문자 반환

```python
print(chr(97)) ➝ a
print(chr(65)) ➝ A
print(chr(13)) ➝ Enter
```

- ord(c) : chr()와 반대로 문자에 해당되는 ASCII 코드 값 반환

```python
print(ord('a')) ➝ 97
print(ort('0')) ➝ 48
```

- divmod(a, b) : a를 b로 나눈 몫과 나머지를 튜플 형태로 반환

```python
print(divmod(7, 3)) ➝ (2, 1)
```

- enumerate(iterable, start = 0) : 시퀀스(리스트, 튜플, 문자열, range)를 인덱스 값을 포함하는 enumerate 객체로 반환(start는 인덱스를 몇 부터 시작할지 지정하는 역할)

```python
for i, name in enumerate(['kim', 'Lee', 'Park'])
    print(i, name) ➝ 0 kim
                      1 Lee
                      2 Park
```

- eval(expression) : 표현식의 연산 결과 반환

```python
print(type(eval('10'))) ➝ <class 'int'>
print(type(eval('3.14'))) ➝ <class 'float'>
print(eval('1 + 2')) ➝ 3 ※ class : int
print(eval('1' + '2')) ➝ 12 ※ class : int
print(eval('1') ➝ eval('2')) ➝ 3 ※ class : int
```

- filter(function, iterable) : 반복 가능한 자료형 요소들이 입력되었을 때 반환 값이 참인 것만 걸러내서 반환

```python
def positive(x):
    return x > 0

print(list(filter(positive, [0, -1, 5, -7, 10))) ➝ [5, 10]
```

- hex(i) : 정수를 '0x' 접두사가 붙은 소문자 16진수 문자열로 변환

```python
print(hex(7)) ➝ 0x7
print(hex(10)) ➝ 0xa
print(hex(1234)) ➝ 0x4d2
```

- id(object) : 객체의 주소 값(Reference) 반환

```python
a = 1

print(id(a)) ➝ 1525378112
```

- input(\[prompt\]) : 입력 받은 문자열 반환

- int(x) : x를 정수로 변환

- len(s) : 시퀀스(문자열, 튜플, 리스트, 딕셔너리, ...)의 길이(요소의 개수) 반환

- list(iterable) : iterable을 리스트로 변환

```python
print(list("12345")) ➝ ['1', '2', '3', '4', '5']
```

- map(function, iterable) : iterable 각 요소가 함수 function에 의해 수행된 결과 반환

```python
def increse(x):
    return x + 1

print(list(map(increase, [1, 2, 3, 4, 5]))) ➝ [2, 3, 4, 5, 6]
```

- max(iterable) / min(iterable) : iterable 중에서 최대/최소값 반환

- open(filename, \[mode\]) : mode(w : 쓰기, r(디폴트) : 읽기, a : 추가, b : 바이너리)로 파일 열기

```python
file_write = open('my_file.txt', 'w')
```

- pow(x, y) : x의 y 제곱한 결과값 반환

```python
print(pow(10, 3)) ➝ 1000
```

- range(\[start,\] stop \[, step\]) : 지정하는 범위의 값을 반복가능한 객체로 만들어 반환

```python
print(range(5)) ➝ range(0, 5)
print(range(0, 5)) ➝ range(0, 5)
print(list(range(0, 5)) ➝ [0, 1, 2, 3, 4]
print(list(range(2, 10, 2)) ➝ [2, 4, 6, 8, 10]
```

- round(number \[, ndigits\]) : 실수를 반올림하여 반환(ndigits는 소수 이하 자릿수를 지정하는 역할)

```python
print(round(3.141592, 2)) ➝ 3.14
```

- sorted(iterable, key = None, reverse = False) : iterable 요소들을 정렬한 후 결과를 리스트로 반환

```python
print(sorted([3, 1, 2])) ➝ [1, 2, 3]
print(sorted("flower")) ➝ ['e', 'f', 'l', 'o', 'r', 'w']
print(sorted("SunFlower")) ➝ ['F', 'S', 'e', 'l', 'n', 'o', 'r', 'u', 'w']
print(sorted("SunFlower", key = str.lower) ➝ ['e', 'F', 'l', 'n', 'o', 'r', 'S', 'u', 'w']
print(sorted("SunFlower", key = str.lower, reverse = True)) ➝ ['w', 'u', 'S', 'r', 'o', 'n', 'l', 'F', 'e']

※ key = str.lower ➝ 대소문자 구분 X
※ reverse = True ➝ 정렬 후 역순
```

- str(object) : 문자열 형태로 반환

- sum(iterable) : iterable의 모든 요소의 합 반환

- tuple(object) : 객체의 자료형 반환

- zip(iterable) : 각 iterable에서 동일한 인덱스의 요소를 추출하여 묶어서 반환

```python
print(list(zip([1, 2, 3], [4, 5, 6]))) ➝ [(1, 4), (2, 5), (3, 6)]
print(list(zip([1, 2, 3], [4, 5]))) ➝ [(1, 4), (2, 5)]
print(list(zip("123", "abc"))) ➝ [('1', 'a'), ('2', 'b'), ('3', 'c')]

keys = ('apple', 'pear', 'peach')
vals = (300, 250, 400)
result = dict(zip(keys, vals))

print(result) ➝ {'apple' : 300, 'pear : 250, 'peach' : 400}
```

- lambda 함수
  - 실행 시 생성해서 사용할 수 있는 익명 함수
  - 입출력이 있는 간단한 한 행짜리 함수를 만들때 사용
  - 형식 : lambda 매개변수 : 표현식

```python
lambda_add = lambda x, y : x + y
lambda_add(3, 5) ➝ 8
```
